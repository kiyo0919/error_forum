<!DOCTYPE html>
<html>
  <head>
    <title>ErrorForum</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/css/lightbox.css" rel="stylesheet">
  </head>
  <header>
    <ul class="nav justify-content-center">
      <% if mentor_signed_in? %>
        <li class="nav-item">
          <%= link_to "投稿一覧", root_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "ログアウト", destroy_mentor_session_path, method: :delete, class:"nav-link" %>
        </li>
        <% else %>
          <li class="nav-item">
          <%= link_to "投稿一覧", root_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "新規登録", new_mentor_registration_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "ログイン", new_mentor_session_path, class:"nav-link" %>
        </li>
      <% end %>
    </ul>
  </header>
  <body>
    <% flash.each do |key, value| %>
      <%=  content_tag(:p, value, class: key) %>
    <% end %>

    <%= yield %>
    <div class="script">
    <script hidden script src='https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/js/lightbox.min.js' type='text/javascript'></script>
    </div>
  </body>
</html>